# Backend Foundation — controller rulings

Decisions taken during subagent-driven execution of
`docs/superpowers/plans/2026-08-26-backend-foundation.md`, without pausing to ask.
Each records what was decided, why, and what it costs if the decision was wrong.

Recorded so the decisions are reviewable rather than lost with the scratch workspace.
The working ledger they came from was git-ignored and has been deleted.

## Rulings, in the order they were made

- **Ruling.** Concurrency capped at 3 implementers, not 9–12 — Tasks 3-7 and 12 each emit an EF Core migration against one linear ModelSnapshot, and 9/11/13/14 have hard predecessor edges; only Tasks 8, 10, 12 are simultaneously independent. Running 12 implementers would produce migration snapshots that cannot merge. — Cost if wrong: less wall-clock parallelism than asked for; no correctness loss.
- **Ruling.** Execution waves — A: 1 ∥ 2. B: 3→4→5→6→7 serial. C: 8 ∥ 10 ∥ 12. D: 9 ∥ 11. E: 13. F: 14. Parallel waves run in isolated git worktrees and I merge between waves. — Cost if wrong: a wave boundary drawn too tight costs a serial task that could have overlapped.
- **Ruling.** Pin the SDK with a global.json at backend/ to 9.0.306 rollForward latestFeature, and install dotnet-ef 9.x as a local tool manifest rather than global. Machine default SDK is 10.0.400; the plan's Global Constraints say .NET 9 / EF Core 9. dotnet-ef is not currently installed at all. — Cost if wrong: one extra file and a tool manifest; trivially reversible.
- **Ruling.** Program.cs DI registrations are the merge surface for parallel waves. Each wave-C/D implementer appends its single AddScoped line; I resolve conflicts at merge rather than serialising the wave. — Cost if wrong: a hand-resolved merge conflict on a one-line addition.
- **Ruling.** Task 1 untracks .env going forward only; POSTGRES_PASSWORD and PGADMIN_PASSWORD remain in history at 015ba58 per spec OD-6. History rewrite is my human partner's call, not mine. — Cost if wrong: secrets stay in git history until they decide otherwise. Flagged, not fixed.
- **Ruling.** Overriding SDD's "never dispatch multiple implementation subagents in parallel (conflicts)" at my human partner's explicit request. Mitigation: every parallel implementer runs in its own git worktree, so no two agents share an index; I merge each wave myself. — Cost if wrong: merge conflicts I resolve by hand, and a wave whose tasks turn out coupled has to be re-run serially.
- **Ruling.** Stop using the Agent tool's isolation:"worktree" flag; create worktrees myself with `git worktree add` from my own branch. Evidence: Task 1's commit 5ca4881 has parent 015ba58 — the flag bases the agent worktree on the MAIN CHECKOUT's HEAD (claude/git-worktrees-585427 @ 015ba58), not on the controller's branch. Wave B is a serial dependency chain; an agent that silently starts from 015ba58 would not see any prior task's work. — Cost if wrong: I manage worktree lifecycle by hand, including cleanup.
- **Ruling.** Task 1's review base is 015ba58, not the 527b41f I recorded as BASE. Same cause. Review package regenerated over the true range. — Cost if wrong: none; the range is now exact.
- **Ruling.** The reviewer's "13 keys / no VITE_*" finding is wrong and is discarded, not carried to the final review. I verified directly: `.env` @015ba58 and `.env.example` @5ca4881 both hold 17 keys, byte-identical key set and order, VITE_API_BASE_URL=http://localhost:5080 present. The implementer's "17 keys" was accurate; the reviewer inferred the key list from rename-diff context lines rather than the whole file. — Cost if wrong: a real key-parity gap would reach the final review unflagged; I checked the full file text, so this is settled, not assumed.
- **Ruling.** Upholding the implementer's unrequested pin of Microsoft.AspNetCore.Mvc.Testing to 9.0.*. The brief gave no version; unpinned it resolves to 10.0.11, which is net10.0-only and would break the .NET 9 constraint. The pin is required for the task to work at all. — Cost if wrong: a version bump to redo, one line.
- **Ruling.** The non-compiling PostgresFixture is plan-mandated, not a defect — the brief states verbatim that it "will not compile until Task 3 creates AppDbContext." Task 3's implementer inherits the obligation to make the test project build. Carried into Task 3's dispatch. — Cost if wrong: Task 3 leaves the suite unbuildable and every later task is blocked; the Task 3 review will catch it.
- **Ruling.** Started Docker Desktop myself rather than stopping to ask. The plan's Global Constraints and Task 1 Step 5 both mandate a running Docker; launching a local dev daemon is reversible and implied by "execute this plan." — Cost if wrong: a GUI app is running on my human partner's machine that they did not personally launch.
- **Ruling.** Upholding the implementer's unrequested edit to MsRelationship.Api.csproj — pinning EF Design to 9.0.19, adding EFCore.Relational 9.0.19, pinning Npgsql to 9.0.4. Floating `9.0.*` across two projects resolved to mismatched assembly versions and produced CS1705 once real EF code existed; the task cannot compile otherwise. Narrow and necessary. — Cost if wrong: pinned versions go stale and need a bump; one line each.
- **Ruling.** The CS0618 warning at PostgresFixture.cs(10,55) — obsolete parameterless PostgreSqlBuilder() — is Task 2's code and outside Task 3's diff, so it is NOT a Task 3 finding and I told the reviewer not to report it. Instead I am carrying it into Task 4's dispatch as an explicit one-line amendment (`new PostgreSqlBuilder("postgres:16")`), because the fixture is shared infrastructure and eleven more tasks would otherwise each inherit a non-pristine build. Fixing it in a task's own dispatch keeps it inside the review loop rather than becoming a silent controller edit. — Cost if wrong: Task 4's diff carries one line that is not in its brief; the Task 4 reviewer is told to expect it.
- **Ruling.** The Important test-isolation finding is REAL and I am acting on it, not dismissing it as plan-mandated. Evidence I checked: the plan uses [Collection("postgres")] in 11 places, and greps for truncate/reset/cleanup/per-test-transaction across the whole plan return NOTHING. Spec §12 requires integration tests over real Postgres via Testcontainers for "identity, merge, archive or history" — the requirements "with the most ways to be subtly wrong". Tests that collide nondeterministically on shared state do not satisfy that; the plan simply never addressed isolation. This is a plan gap, not a plan decision, so the spec governs. — Cost if wrong: a fixture method and one line per test class that turn out unnecessary; cheap. Cost of NOT doing it: nine further tasks write tests against an accumulating database, and the collisions surface as intermittent failures attributed to whichever task runs last.
- **Ruling.** Remedy = add `ResetAsync()` (TRUNCATE all tables) to PostgresFixture and have every test class call it from InitializeAsync. Folded into Task 4's dispatch together with the CS0618 constructor fix, so both fixture changes pass through Task 4's review seat rather than becoming unreviewed controller edits. Retrofitting TaxonomyTests is part of that. Every later dispatch carries the standing instruction to adopt ResetAsync. — Cost if wrong: Task 4's diff is wider than its brief; its reviewer is told to expect exactly these two additions.
- **Ruling.** Resumed the same implementer to run the suite in the foreground, commit, and write its report, rather than committing its work myself. A controller commit would skip the review seat and put unreviewed work on the branch under my name. — Cost if wrong: one extra round-trip if the agent cannot recover its own context.
- **Ruling.** Adopt snake_case column naming project-wide rather than patching each raw-SQL snippet as it fails. The spec is the binding authority and §5 names every column in snake_case; the plan's own literal SQL assumes it; the implementation simply never configured it. Patching per-task would force four-plus implementers to each diverge from their brief's literal text, with a fresh chance to get the quoting subtly wrong. — Cost if wrong: one added dependency (EFCore.NamingConventions) and one rename migration. Cost of NOT doing it: Tasks 5 and 6 fail the same way Task 4 did, and every later raw-SQL snippet is a trap.
- **Ruling.** Sequencing — do it as a focused corrective task (4b) after Task 4's review clears and before Task 5, not folded into Task 5. A dedicated diff and its own review seat beat a Task 5 diff that mixes a project-wide rename with new schema. Task 4's quoted-column workaround reverts to the brief's literal text as part of 4b. — Cost if wrong: one extra dispatch and review cycle.
- **Ruling.** The untested empty-string-email branch IS worth closing, and I am carrying it into Task 8's dispatch rather than deferring it to the final review. The computed column has two routes into the fallback branch — `email IS NULL` and `btrim(email) = ''` — and the brief's four mandated tests exercise only the NULL route. This is the identity backbone (FR-18): if the empty-string route is broken, a profile with email "" silently gets an email-based key instead of the name|org fallback, and dedup misbehaves in a way no test would catch. Task 8 is the duplicate-matching task and consumes identity_key directly, so one added test lands naturally in its scope at no extra dispatch cost. — Cost if wrong: one redundant test in Task 8's diff. Cost of NOT doing it: an untested branch in the identity key that Tasks 8, 9 and 14 all build on.
- **Ruling.** The Important boundary-coverage finding is real and I am closing it, not parking it. The suite proves ±4 are REJECTED but only ever proves 1, 2, 3 ACCEPTED — never -3 or 0. A predicate mistyped as `score > -3 AND score <= 3` would pass the suite unchanged. The reviewer independently verified Postgres BETWEEN is inclusive, so there is no live defect today; the hole is in the regression net for FR-01, the -3..+3 scale that is the product's central business rule. The fix is two assertions. — Cost if wrong: two redundant assertions. Cost of parking: the plan's most central invariant rests on a boundary no test exercises.
- **Ruling.** Delivered as an addendum to Task 7's in-flight dispatch rather than a new dispatch or a controller edit. Task 7 is the closest live task touching relation score writes, it was dispatched minutes earlier so the interruption is cheap, and routing it through an implementer keeps it inside a review seat. — Cost if wrong: Task 7's diff carries two assertions in Task 6's test file; its reviewer is told to expect them.
- **Ruling.** Adopting that recommendation as a standing instruction rather than treating it as a defect to fix now. Making the DbSets non-public would require editing Task 6's committed tests, and the Global Constraint is about controllers, which do not exist yet. I have written "inject RelationWriter, not AppDbContext, for relation mutations" into the Task 10 and Task 12 dispatches, and it goes into Task 9's too. — Cost if wrong: enforcement stays conventional and a future contributor could bypass the writer via AppDbContext; the final whole-branch review is pointed at this.
- **Ruling.** BOTH Important findings are real and neither is dismissed — but both are deferred to a dedicated corrective task 7b, dispatched after Wave C merges and before Wave D. NOT parked, scheduled. Reasons: (a) both fixes touch AppDbContext and/or RelationWriter, and Task 12 is concurrently modifying AppDbContext in its own worktree right now — fixing in place would guarantee a three-way conflict against three in-flight agents; (b) neither is a live defect today: no code path updates or deletes history rows, and there are no concurrent writers in a test suite or an unshipped backend; (c) adding a concurrency token changes RelationWriter's exception behaviour (callers would newly face DbUpdateConcurrencyException), and Tasks 9/10/12 are being written against its current contract at this moment — changing it mid-wave would invalidate work in progress. — Cost if wrong: the audit trail keeps a theoretical concurrency hole for the length of Wave C, on a backend with no users. Cost of fixing now: near-certain merge conflicts across three parallel worktrees plus invalidated in-flight work.
- **Ruling.** This situation is itself the cost of my earlier decision to overlap each task's review with the next task's dispatch. The overlap bought real wall-clock time across seven serial tasks; the price is that an Important finding can land after dependent work has started. Recording it so the trade is visible rather than implicit — and 7b is the mechanism that keeps it from becoming a silent deferral.
- **Ruling.** The no-op STAYS; I am not making ApproveAsync throw on a missing relation. Reasoning: the desired end state (no relation) is already true; the SubmissionItem row is itself the durable record of what was requested and approved; and failing loudly on a benign already-done case would be worse for the approving admin than a documented no-op. What is unacceptable is that the behaviour is undocumented and unproven — so it gets pinned by tests and a comment instead. — Cost if wrong: an auditor reconstructing "what did this approval change?" finds a SubmissionItem with Action=Remove and no corresponding history row. Discoverable inconsistency, not silent corruption, and the submission record still carries the intent.
- **Ruling.** Fix confined to SubmissionService, SubmissionTests and comments. RelationWriter and AppDbContext are explicitly OFF LIMITS for this fix round because Task 7b is editing both right now — the two agents run in different worktrees and must not touch the same files. — Cost if wrong: a follow-up round if the fix genuinely needs writer changes.
- **Ruling.** Resumed the same agent rather than re-dispatching fresh. Its context is intact and the interruption was environmental — treating a sleep-induced API error as a model failure would throw away correct work and repeat it at full cost. This is NOT a fix round; the review loop has not started for 7b. — Cost if wrong: one wasted round-trip if the agent cannot recover its own context, after which I would re-dispatch with the brief and the partial diff.
- **Ruling.** Use the EXISTING `Relation.UpdatedAt` as the concurrency token — `.Property(x => x.UpdatedAt).IsConcurrencyToken()` — rather than adding any column. Reasoning: spec §5 defines relations as exactly id, columbus_user_id, ms_profile_id, score, note, updated_at plus the unique pair; the spec is the binding authority, and adding a `version` column would deviate from it to solve a problem an existing column already solves. RelationWriter.UpsertAsync already stamps UpdatedAt unconditionally on line 48 for BOTH the create and update paths, so the token is always fresh. EF will put `updated_at = @original` in the UPDATE's WHERE clause; a concurrent writer's change makes zero rows match and raises DbUpdateConcurrencyException. No migration adds anything. — Cost if wrong / accepted limitation, stated plainly and NOT oversold: two updates landing within the same clock tick carry identical UpdatedAt and would not be caught. Vanishingly unlikely at DateTimeOffset.UtcNow resolution, and strictly better than the zero protection that exists today. The implementer is instructed to record this limitation honestly in its report.
- **Ruling.** Commit the empty migration TOGETHER with the snapshot change rather than discarding it. The snapshot now records the concurrency token; discarding the migration while keeping the snapshot would leave the two inconsistent and make the next `migrations add` regenerate an empty diff. An empty migration is the honest record: the model changed, no DDL was required. — Cost if wrong: one no-op migration in the history, which is self-documenting.
- **Ruling.** Re-dispatched fresh rather than resuming, and CHANGED the dispatch rather than retrying it verbatim. The agent died during exploratory file-reading, so I inlined the exact shapes it was gathering — MsProfilesController's signature, RelationWriter's two method signatures, MsProfile and Relation field lists with their constraints, the RelationChangeType members, and Seed's three helpers. Re-running an identical prompt that stalled during exploration invites the same stall; removing the exploration removes the exposure. — Cost if wrong: a longer dispatch prompt, and a small risk the inlined shapes drift from the real files (they were read directly from the merged branch moments before dispatch).
- **Ruling.** HOLD Task 11 rather than re-dispatch it now. Running a third agent into a visibly degraded environment is how two clean worktrees became two dead agents. Task 9 and Task 11 are genuinely independent and I would normally run them together — this is a deliberate, temporary departure from the parallelism my human partner asked for, justified by observed instability rather than caution in the abstract, and reversed the moment Task 9 lands. — Cost if wrong: Task 11 runs serially after Task 9 instead of alongside it, costing wall-clock but no correctness.
- **Ruling.** When Task 11 is re-dispatched, use the same remedy as Task 9's retry — inline the exact shapes (ColumbusUsersController's current actions, UserArchiver's SuperAdmin guard, ColumbusUser's field list, Seed's helpers) so it does not spend its first minutes crawling files, which is where both stalls happened. — Cost if wrong: a longer prompt.
- **Ruling.** STOP dispatching. The retry already tested my hypothesis that exploratory file-reading was the trigger — I inlined every interface shape so it would not need to crawl, and it died identically. That falsifies the hypothesis and means the failure is environmental, not prompt-shaped. Dispatching again would repeat an action whose outcome I can already predict, which is exactly what the skill's "never force a retry without changing something" rule forbids — and I have run out of meaningful changes to make. Surfacing to my human partner instead. — Cost if wrong: the run pauses when it could have continued; trivially resumed, since all state is committed and the ledger records exactly where to pick up.
- **Ruling.** Resume with STRICTLY ONE agent at a time instead of the 3-4 concurrent used before the failure. That is the one meaningful change available after the inlined-shapes retry falsified my prompt-shape hypothesis — if load or concurrency drove the watchdog kills, serialising removes it; if a single agent also dies, the fault is unconditional and I stop again with better evidence. Starting with the LIGHTEST possible probe: Task 7b's outstanding review, which needs no Docker, no dotnet, and no Testcontainers, so it isolates the agent-runtime question from the container-load question. — Cost if wrong: the remaining four tasks run sequentially rather than in two parallel waves, costing wall-clock but no correctness.
- **Ruling.** The Important finding lands against my own ruling and I am acting on it, not defending the ruling. But I am NOT applying the fix on speculation — there is real uncertainty about whether the bug exists at all, because Npgsql may truncate the comparison PARAMETER symmetrically with the stored value, in which case the WHERE matches and the finding is theoretical. Dispatched fix round 1/5 with reproduction ordered BEFORE the fix: construct a Relation whose UpdatedAt carries deliberate sub-microsecond residue (tick count not divisible by 10), write it, then write again through the SAME DbContext with no intervening load, and observe whether DbUpdateConcurrencyException is thrown. Report the observation as the headline. — Cost if wrong: one test that documents a non-bug, which is still worth having as a regression guard.
- **Ruling.** Apply the precision fix REGARDLESS of what the reproduction shows. A suite whose determinism depends on whether the machine's clock yields tick counts divisible by 10 is a bad property independent of whether the bug currently manifests, and removing the whole class of uncertainty costs about one line. — Cost if wrong: a redundant truncation call.
- **Ruling.** PARK this for the final whole-branch review rather than spend a dedicated fix round on it now. Reasoning: the underlying bug is already established as NOT REPRODUCIBLE, the fix is defensive rather than corrective, the re-reviewer explicitly judged it non-blocking, and three substantial tasks (9, 13, 14) remain in a pipeline I have deliberately serialised after the watchdog failures. Spending a round here would cost more than the risk it removes. — Cost if wrong: a future accidental revert of TruncateToMicroseconds goes undetected by the suite, reintroducing a defect that does not currently manifest anyway.
- **Ruling.** Dispatched on OPUS rather than sonnet, unlike every other implementer this run. Justification per the skill's model-selection guidance — this is the one task with genuine architecture/design content rather than transcription: the unique-index collision policy is an unforced choice with real consequences for the audit trail, and the tombstone-graph edge cases (merging into self, merging an already-tombstoned profile, a survivor that is itself tombstoned, chained D→S→T) are ways to corrupt the identity graph if handled carelessly. It has also failed twice already, so a capability bump is cheap insurance. — Cost if wrong: a more expensive implementer on one task.
- **Ruling.** Fix both. For Important 1, steered toward a CONDITIONAL UPDATE (WHERE ... AND role = 'SuperAdmin', assert one row affected) over SELECT ... FOR UPDATE — not for correctness, both work, but because the conditional update is DETERMINISTICALLY TESTABLE with the two-context no-threads pattern already used by Concurrent_updates_to_the_same_relation_are_caught. A fix I cannot test deterministically is a fix I cannot defend. — Cost if wrong: locking would also have worked and might be marginally simpler.
- **Ruling.** For Important 2, explicitly forbade manufacturing a fake rollback scenario to rescue the claim. If atomicity is by-construction from the single flush — and the reviewer's reading, which I share, is that no reachable path fails between the two writes — then the honest remedy is to correct the claim and rename the test, not to invent a failure mode. Also asked it to decide openly whether the explicit transaction is load-bearing or defensive and to say which. Noted that if its Important-1 fix introduces a second flush, atomicity may BECOME load-bearing and a real rollback test then becomes possible — asked it to work that through rather than assume. — Cost if wrong: a test named for a weaker property than someone might hope for, which is strictly better than one named for a property it does not have.
- **Ruling.** APPROVED the optional `changeType` parameter added to RelationWriter, which is shared code consumed by SubmissionService and UserArchiver. Verified first-hand: it defaults to null, behaviour is preserved via `changeType ?? existing-logic`, and both SubmissionService call sites are byte-identical. The implementer's argument is correct and the plan implicitly required this — RelationChangeType.MergeMoved and MergeDiscarded have existed since Task 7 precisely for this task, and without the parameter you cannot simultaneously satisfy "every write goes through the writer" and "merge history is labelled MergeMoved/MergeDiscarded". Told the reviewer not to re-litigate whether it should exist, but to judge whether it is implemented correctly and whether the labelling is right. — Cost if wrong: a shared-code signature change that later callers must understand; mitigated by the default and by extensive XML docs.
- **Ruling.** Dispatched the review on OPUS rather than sonnet — 719 insertions of core identity logic, a change to shared code, and tombstone-graph edge cases (self-merge, already-tombstoned, tombstoned survivor, D→S→T chains, empty mergeIds, mergeIds containing the survivor) where a mistake corrupts the identity graph silently. Scaled the reviewer to the diff's risk as the skill directs. — Cost if wrong: a more expensive review seat on the single highest-risk diff of the run.
- **Ruling.** PARK the concurrency Minor rather than elevate it to a fix round, despite it being the same class of bug I DID fix as Important in Task 11. Reasons: the reviewer traced the code and rated it Minor on the merits; merging is an admin-triggered, rare operation; and overriding a reviewer's severity upward spends budget against that reviewer's own considered judgment, which is as much a distortion of the process as ignoring a finding would be. — Cost if wrong: a rare identity-graph corruption where a tombstone points at one survivor while its relations landed on another. Precise remedy, already proven in this codebase by Task 11's fix: a conditional ExecuteUpdateAsync on the tombstone write with a `WHERE merged_into_id IS NULL` predicate plus an affected-row check, or a SELECT ... FOR UPDATE on the duplicate row inside the existing transaction. Surfacing this to my human partner explicitly rather than leaving it in the ledger alone.
- **Ruling.** Seeding all four mockup arrays, not just the two in the brief's abbreviated sample Seeder.cs, is CORRECT. Verified against the brief's own Step 1, which names state.domains, state.msProfiles, state.columbusProfiles and state.relations explicitly and asks for the c1 and r1 ids preserved as legacyId — the c1/r1 prefixes are exactly columbusProfiles and relations. The sample was abbreviated, not exhaustive. Told the reviewer not to flag it as scope creep but to judge whether the users and relations are seeded WELL. — Cost if wrong: a seeder loading more than intended, which is trivially reversible.
- **Ruling.** legacyId living only in seed.json rather than as a database column is ACCEPTED. The brief's phrasing — keep the ids "as a legacyId field so the mapping stays checkable" — is genuinely ambiguous about where, and persisting it would have required the schema change I explicitly disallowed. Asked the reviewer to judge whether the mapping is genuinely checkable as implemented and what is lost. — Cost if wrong: cross-referencing mockup records to database rows needs a Name/Email join rather than an id lookup.
- **Ruling.** RULED on concern 3 — seeding relations directly rather than through RelationWriter STANDS. I deliberately did not pre-judge this and the reviewer's reasoning is what decides it: RelationWriter.ChangedByUserId is a required non-nullable Guid with no system-actor concept anywhere in the domain, so routing seed data through it would fabricate a real person as the author of 31 relationships — planting a false attribution claim in the one table whose entire purpose is trustworthy attribution. An honestly-empty history for pre-application data is the better outcome. Notably the reviewer also found that the UserArchiver precedent I offered as the "against" argument actually cuts the OTHER way, and said so rather than taking the easier symmetry — and it confirmed no technical invariant is violated, since the append-only guard only forbids MUTATING existing history rows, not requiring every relation to have one. — Cost if wrong: relation_history has no rows for the 31 seeded relationships, so a timeline view shows them appearing without provenance. Strictly better than false provenance.
- **Ruling.** I handed both claims to the reviewer to CONFIRM OR REFUTE rather than directing a fix on my own reading. I have been wrong once already this run — the timestamp-precision finding turned out non-reproducible — and an authorization call is exactly where I want a second pair of eyes before acting. Told it explicitly not to simply agree with me. — Cost if wrong: one review cycle before fixing a hole I am already fairly confident of.
- **Ruling.** OVERRULING the literal brief text on Critical 1. The brief named "the approval endpoint" and the implementer read it faithfully, but approve and reject are two halves of one moderation decision writing the same three fields on the same controller. The brief's intent does not survive its literal reading. Fix: [Authorize(Policy = "CanEdit")] on Reject, AND invert the test that ratifies the hole. — Cost if wrong: an endpoint gated more tightly than the brief's letter requires, which is the safe direction to err on a security boundary.
- **Ruling.** FIX the plan-mandated null-oid identity confusion (Important 4) rather than park it. EF translates `u.EntraObjectId == objectId` to `entra_object_id IS NULL` when the parameter is null, so the predicate matches every user who has never signed in; one such row that is not the email's owner binds the caller to the WRONG ColumbusUser, inheriting its Id and Role. Not reachable today since real Entra v2 tokens always emit oid — but the failure mode is identity confusion rather than denial, it sits on the security boundary, and the guard is one clause. — Cost if wrong: one redundant null check.
- **Ruling.** Also fixing the floating `Version="3.*"` on Microsoft.Identity.Web (Important 3). Every other package here is pinned exactly after an earlier CS1705 mismatch, and a non-reproducible restore on the one library implementing TOKEN VALIDATION is the worst possible place for it. And the Production-only startup guard (Important 5), because the placeholder credentials fail closed but not LOUDLY — the app boots clean and /health returns 200 while 100% of authenticated traffic fails, so a health-gated deploy reports green with auth entirely dead.
- **Ruling.** ONE fix wave, per the skill — not one fixer per finding. Contents: the two races, the missing POST /api/ms-profiles tests, the false docstring, ConnectionStrings__Default in .env.example, FR-02's two-line assertion, and CanEdit on GET /api/submissions. — Cost if wrong: a slightly wider fix wave than the strict must-fix list.
- **Ruling.** INCLUDING Minor 3's GET /api/submissions gate in the wave despite the reviewer rating it acceptable-until-Plan-4. It is one attribute, and it is the read side of exactly the moderation queue whose write side (Reject) I just held the merge for — every authenticated Standard user can currently read every other user's proposed scores and notes. Fixing the write half and knowingly leaving the read half open would be the same asymmetry the reviewer just criticised me for. — Cost if wrong: one endpoint gated tighter than Plan 1 required.

## Findings deferred to future work

Raised by a task review, judged non-blocking, and triaged as acceptable by the final whole-branch review.

- Task 1: minor (deferred): reviewer claimed .env.example has 13 keys and no VITE_* block — FALSE POSITIVE, disproved below.
- Task 1: minor (deferred): report's pasted docker output shows container names from the agent's own worktree, so the health claim is not independently re-verifiable from that text alone. Resolving by direct observation instead.
- Task 2: minor (deferred): backend/.gitignore duplicates bin/ and obj/ from the root .gitignore (also adds .vs/ .vscode/ .idea/ .DS_Store, which the root lacks). Harmless overlap.
- Task 2: minor (deferred): backend/.config/dotnet-tools.json has no trailing newline.
- Task 3: minor (deferred): test csproj still floats Microsoft.AspNetCore.Mvc.Testing at 9.0.* while the API project pins EF exactly. Reviewer confirmed Mvc.Testing's graph cannot pull EF Core, so it cannot reproduce the CS1705 class of failure. Hygiene gap only. Central package management is worth considering — flagged for the final review.
- Task 4: minor (deferred): DepartmentId has no database FK constraint and no index — nothing prevents an orphaned DepartmentId pointing at a deleted department. Reviewer confirmed this is plan-mandated: the brief's own Step 4 snippet omits any FK configuration. Relevant to whichever later task adds the navigation. Flagged for the final review.
- Task 4: minor (deferred): no invariant ties Status == Archived to ArchivedAt being non-null. Not requested by the brief.
- Task 4b: minor (deferred): the implementer's own report miscounts its migration (says RenameColumn x24, actual 23; RenameIndex x4, actual 5 — it omits ix_columbus_users_email from the tally). Generated code unaffected; prose bookkeeping slip only. Note my earlier census of 46/10 counted Up and Down together — 23 and 5 per direction. Consistent, not a contradiction.
- Task 5: minor (deferred): identity-key collision semantics — two people with the same name and no email at one organisation collide; a person who later gains an email moves from name|org to email, briefly yielding two live keys. Reviewer judged both inherent to the brief's exact-match design and precisely what Task 8's fuzzy pg_trgm matching exists to close. By design, not a defect.
- Task 6: minor (deferred): ix_relations_ms_profile_id is EF's standard auto-generated FK support index, not requested but harmless.
- Task 6: minor (deferred): Seed.ProfileAsync takes an optional name override, Seed.UserAsync does not — asymmetry inherited verbatim from the brief; extensible non-breaking.
- Task 6: minor (deferred): Relation's FKs are non-nullable Guid while every other inter-table reference in the schema is Guid? — a deliberate and correct departure, since a relation without both endpoints is meaningless.
- Task 8: minor (deferred): Create has a check-then-insert race — two concurrent requests with the same identity key could both pass the SingleOrDefaultAsync check, and one then hits the unique index as an unhandled DbUpdateException (500) instead of a clean 409. The partial unique index still prevents a duplicate row, so data integrity holds; only error UX degrades. Plan-mandated, verbatim from the brief's Step 4.
- Task 8: minor (deferred): CreatedAtAction points at Create itself since no GET-by-id exists, so the Location header is not fetchable. Cosmetic, plan-mandated.
- Task 8: minor (deferred): no index supports the trigram similarity scan. Acceptable against spec OD-4's ~2,000-profile ceiling; the brief did not ask for one.
- Task 10: minor (deferred): GET list endpoint returns raw ColumbusUser entities rather than a DTO, leaking EntraObjectId. Plan-mandated, verbatim from the brief.
- Task 10: minor (deferred): actorId as a query-string parameter on a state-mutating POST is unconventional; interim only, Task 13 replaces it.
- Task 10: minor (deferred): the Super Admin guard's InvalidOperationException surfaces as a 500 rather than a 4xx. No error-handling layer exists yet.
- Task 12: minor (deferred): no FK constraints on Submission/SubmissionItem, diverging from Relation's FK-with-Restrict style. Brief's Step 3 does not ask for them.
- Task 12: minor (deferred): score range not validated at submit time — an out-of-range score fails only at approve time via ck_relations_score, surfacing as a 500 to the approving admin rather than a validation error at submission.
- Task 12: minor (deferred): reject-after-approve untested (approve-after-reject is tested; the guard is symmetric).
- Task 7b: minor (deferred): the implementer's report tally names 2 of the 3 new SubmissionTests methods from the intervening merge, omitting Rejecting_after_approve_throws. Total of 38 is still correct. Cosmetic.
- Task 9: minor (deferred): a merge-aware history view must UNION across tombstones. Pre-merge history stays on the duplicate's id, and the survivor-wins collision writes its MergeDiscarded row on the DUPLICATE's id (the brief put it on the survivor's). Each choice is individually more truthful — the survivor's row genuinely did not change — but together they mean `WHERE ms_profile_id = @survivor` no longer returns the full merge story. Whoever builds the timeline read needs this; currently it is stated only in two XML comments.
- Task 9: minor (deferred): the "already merged elsewhere" guard is NOT concurrency-safe — plain READ COMMITTED read, no row lock, and MsProfile has no concurrency token. Two simultaneous merges of the same duplicate into different survivors can both pass; the second UPDATE blocks then overwrites, leaving a tombstone pointing at survivor B while its relations committed onto survivor A.
- Task 9: minor (deferred): refusal is order-dependent within one mergeIds list — [A, B] where B→A succeeds, [B, A] throws. Documented in XML rather than accidental, and refusing is the safe direction; sorting live-first would remove the wart.
- Task 9: minor (deferred): The_duplicate_is_tombstoned_not_deleted reads through the tracked context, so it would pass on an in-memory-only mutation. It is the brief's test verbatim and persistence IS covered by three fresh-context tests, so nothing is actually unguarded.
