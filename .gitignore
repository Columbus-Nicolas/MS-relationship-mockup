# Local tooling / dev-server config
.claude/

# Local environment — never committed. Copy .env.example to .env and fill it in.
.env
.DS_Store
bin/
obj/

# Scratch directory for the SDD process — never committed.
.superpowers/
